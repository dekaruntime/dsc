[package]
name = "core"
version.workspace = true
edition.workspace = true
description = "CLI registry, args, and context for dsc"
license.workspace = true

[dependencies]
deka-cli-core = { version = "0.4.2", default-features = false, features = ["registry"] }
