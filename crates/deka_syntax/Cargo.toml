[package]
name = "deka_syntax"
version.workspace = true
edition = "2021"
description = "DekaScript lexer, parser, and typechecker (Compiler v2)"
license = "Apache-2.0"

[dependencies]
bumpalo = { version = "3.16", features = ["collections"] }
serde = { workspace = true }

[dev-dependencies]
insta = "1.40"
