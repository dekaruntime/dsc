import { echo } from "io"

const n = 7
const el = <span>{n}</span>
const result = unsafe { deka.ui.renderToString(el) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e.message),
  _ => echo("nomatch"),
}
